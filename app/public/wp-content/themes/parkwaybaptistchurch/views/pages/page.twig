{% extends "base.twig" %}

{% block content %}

    <div class="container max-w-xl mx-auto pt-20 pb-20">
        <h1 class="font-medium text-mega">{{ post.title }}</h1>
        <div class="body-content">
            {{ post.content }}
        </div>
    </div>

{% endblock %}